node_modules/
.next/
.next-*/
data/*.db
data/*.db-*
*.tsbuildinfo
next-env.d.ts
.DS_Store
.worktrees/
.playwright-mcp/
claude code images/
dir-sidebar.jpeg

# Firecrawl scratch (scraped API docs, tokens) — never commit.
.firecrawl/

# FounderOS Microsoft Agent Framework sidecar.
.agent-workspace/
.venv/
**/__pycache__/
*.py[cod]
services/agent-runtime/.pytest_cache/

# Secrets — never commit real credentials. Only .env.example is tracked.
.env
.env.local
.env.*.local
